# ラウンド1 変更記録（2026-09-25）

2026-09-24 の独立監査（[`2026-09-24-audit-ja.md`](2026-09-24-audit-ja.md)、コミット `2cf4eba` で 65/100 点）の指摘に対して、ラウンド1で行ったことの記録です。各指摘は直す前に再現を試み、状態・コミット・証拠・影響する採点項目を記録しました。

- 作業ブランチ：`claude/audit-round-1-yh5awi`（ご指定は `audit-round-1` でしたが、この作業環境では push できるブランチがこの名前に指定されていたため、こちらを使いました。中身は同じ意図のブランチです。`main` への push・マージ・タグ付け・npm 公開・公開ワークフローの実行は一切していません）
- 基点：`2cf4eba`（監査対象と同じコミット）
- 次の設計：[`docs/design/round-2-scoring-and-evaluation.md`](../design/round-2-scoring-and-evaluation.md)

## 用語メモ（初出のみ）

- **コミット**：変更をひとまとまりで記録したもの。`c8cd4ee` のような英数字（ハッシュ）で指します。
- **回帰テスト**：一度直した不具合が再発しないことを確かめる自動テスト。今回は「直す前のコードでは失敗し、直した後は通る」ことを確認してから追加しました。
- **オフライン**：API キーを外し、送信先（`TYPESAFE_BASE_URL`）を閉じたローカルポート `http://127.0.0.1:9` に向けた状態。万一送信しようとしても外部には出ません。
- **ライブテスト**：実際の Jev API を呼ぶテスト（ファイル名が `.live.test.ts`）。キーが無いと自動的にスキップされます。
- **dist**：プラグインの配布用にビルドした JavaScript。Claude Code はビルドせずにそのまま使うため、コミットしておく必要があります。

## 状態の凡例

| 状態 | 意味 |
| --- | --- |
| 修正済み | 再現し、直し、テストや出力で確認した |
| 一部修正 | 指摘の一部だけを直した（残りの扱いを併記） |
| 再現せず | 指摘どおりには再現しなかった（証拠を併記） |
| 保留 | このラウンドの権限・範囲では実行できない（あなたの操作が必要など） |
| ラウンド2へ | 採点方式や評価の設計変更が必要なので、設計提案に回した |

## 最終確認の結果（2026-09-25、Node 22.22.2）

| 確認 | 結果 |
| --- | --- |
| `pnpm install --frozen-lockfile`（ビルド生成物と `node_modules` を消した状態から） | 成功 |
| `pnpm build` | 成功 |
| dist の鮮度（`git diff --exit-code -- packages/claude-plugin/dist`） | 差分なし |
| `pnpm lint` | エラー0・警告0 |
| `pnpm typecheck`（テストファイルを含む型検査、今回新設） | エラー0 |
| `pnpm test`（オフライン） | 329件合格・14件スキップ（ライブ）・失敗0（監査時は217件合格）。core 208、CLI 54、MCP 16、プラグイン 51 |
| 同上を Node 20.20.2 で | 同じく全件合格、型検査エラー0 |
| ライブテスト（キーあり、合成データのみ） | 14件すべて合格（core 8、MCP 4、プラグイン 2） |
| `pnpm audit` | `No known vulnerabilities found`（監査時は7件） |
| `node examples/eval-tasks/verify.mjs` | 16/16 ok |
| `node eval/tasks.mjs --selftest` | 16/16 ok |
| `node eval/check-docs.mjs` | README・PREREGISTRATION.md・設計提案の評価数値がすべて保存データと一致 |
| `node scripts/check-versions.mjs` | **意図的に失敗**：7ファイルの版は 0.6.0 で一致。MCP 設定例の `ctxjev-mcp@0.5.0` の固定が 0.6.0 と異なるため。npm にある最新版を指すための意図した状態で、0.6.0 を公開するときに `--update-pins` で更新する（公開ワークフローがこれを強制する） |

## 費用（API が返した使用量から）

- **Claude API：$0**。有料の評価（`tasks.mjs`・`outcome.mjs`・`plugin.mjs` の実行）は行っていません（`--report`・`--selftest` のみ）。
- **Jev：合計 約 2,037,249 入力トークン、約 $0.086**（Jev の公開価格 $0.042／100万入力トークン、出力は無料）。内訳：
  - `eval/run.mjs`（保持率評価、合成・記録済みの評価用会話のみ）：4回。保存した2回（使用量は各結果ファイルの `jevUsage`：dev 108要求・683,328トークン、ホールドアウト 54要求・316,767トークン）と、同じコードをコミット前に実行した2回（同じ量、`eval/results/superseded/`）。コミット前の2回は、保存ファイルに記録されるコミットと実際のコードを一致させるためにやり直したもので、その結果も隠さず後述します。
  - ライブテスト：2回、合計22要求・35,769トークン（ローカルの中継を通して API の応答に含まれる使用量を合計）。
  - README の出力例の取得：1要求・1,290トークン。

## 改善案（監査レポート第7節）ごとの状態

| # | 内容 | 状態 | コミット | 証拠 | 影響する採点項目 |
| --- | --- | --- | --- | --- | --- |
| 1 | 副指標の数値の不一致・未保存・ランダム基準の不記載・構造的な0% | 修正済み（数値の保存・生成・CI 検査・ランダム順とラベル順の併記・「ランダム順以下」の明記）。探索的指標を追加（事前登録の指標は不変） | `630072e` `5773cb1` `b81bc76` | `eval/results/retention-{dev,holdout}.json`（コミット `630072e`、未コミットの変更なし）。`node eval/check-docs.mjs` が一致を確認。数値を1つ書き換えると失敗し、生成されていない表を足しても失敗することを確認済み | 3b、3a、3c |
| 2 | マスキングの見逃し・切り詰め後マスクによる部分漏洩 | 修正済み | `c8cd4ee` `f93362c` `8d2c701` | `scripts/redact-coverage.ts`：`2cf4eba` は 20/40 形式を隠し無害22例中4例を誤って変更 → 現在 40/40・0/22。テスト「redactSecrets: the audit’s secret formats」（40件）、「leaves ordinary text alone」（22件）、「toolEntryContent > masks a tool input before cutting it short…」（修正前は失敗） | 6a、5b |
| 3 | README と公開版の不一致・プラグインの未審査配布 | 一部修正（README に npm 0.5.0 と main の違いを明記）。公開は**保留**（あなたの操作）。配布の固定は**ラウンド2へ**（調査済み） | `b2ad630` | README 冒頭の注記と「What's on npm vs. main」の表。Claude Code 2.1.282 同梱のスキーマで `git-subdir` 取得元（`ref`・`sha` 指定可）を確認。今切り替えると v0.5.0 へのダウングレードになるため見送り（設計提案 第2節） | 7a、8a、1a |
| 4 | ローカルデータの平文・権限・削除 | 修正済み | `616a38c` `739c63a` | 再現：キャッシュ 0644・ディレクトリ 0755・秘密が平文 → 修正後 0600・0700・平文なし。テスト「loadFileScoreCache > keeps the file readable only by the user…」「drops keys an earlier version stored in plain text…」「keeps at most maxEntries…」「cacheKeyFor > is a SHA-256 digest…」「removeLegacyState > removes only its own snapshots…」「preCompact.js (dist) > masks a secret in a /ctxjev:set-goal goal…」（修正前の dist では失敗） | 6b |
| 5 | 負の節約量・スタック溢れ | 修正済み | `7da04b0` | 再現：既定設定で `savedTokens: -35`（監査の例とは別の会話で同じ現象）、15万ツール呼び出しで `RangeError`（`targetTokens` 指定時。指定なしでは溢れなかった）。テスト「pruneMessages > never makes a conversation larger > …」4件と、ランダム入力の性質テストに「節約量は負にならない」「独立に数えた大きさが増えない」を追加（修正前は −13 の例を検出して失敗） | 1b、5b |
| 6 | `local` 採点の閾値不整合 | ラウンド2へ（文書化のみ実施） | `b2ad630` | `DEFAULT_POLICY` の説明に「`local` では多くが削除される」と明記。設計提案で「予算内の上位から残す」方式への置き換えを提案 | 1c、2b |
| 7 | テストの衛生 | 修正済み | `90296a9` `198e626` `041fa38` `6b0432a` | 締め切りテストは、要求を受けて応答しないローカルサーバーに向け、要求がそこに届いたことと 0.5 秒の締め切りで終わったことを確認。サブプロセスには許可リストの環境変数だけを渡す（`test-support/subprocessEnv.ts`）。`pnpm typecheck` を CI に追加し既存の型エラー（`recencyWeight.live.test.ts:38`）を修正。Jev クライアントを差し替え可能にし、キャッシュの当たり外れ・応答欠落・使用量の集計をオフラインで検査（「scoreRelevance with an injected client (offline)」6件）。既定が recency であることのテスト（「the default scorer > is recency…」）。キー無しの MCP `listTools` テスト | 5a、5c、8b |
| 8 | 古い・不正確な記述 | 修正済み | `15727d2` `b2ad630` `b81bc76` `e9341d8` | 「約100ms」削除、「50件でも1件とほぼ同じ費用」を訂正、出力例を実際の既定（recency）と `--scorer jev` の出力に差し替え、「inspection skills が2つ」を訂正、「ホールドアウト未実施」を訂正（README・プラグイン README。PREREGISTRATION.md は計画部分を書き換えず「登録後の変更」に注記）。`--version` はコマンドの後でも動くよう実装（テスト「ctxjev flags > prints the version before or after the command」） | 7a、3b、1a |
| 9 | 開発依存の脆弱性・供給網 | 修正済み | `a19f2d3` `b2a18fc` `7619a61` | vitest 2.1.9→4.1.11（5.x は Node 22.12 以上が必要で、CI の Node 20 と合わないため見送り）、esbuild 0.21→0.28。`pnpm audit` 0件。Dependabot 設定。Actions をコミット SHA で固定、publish.yml の npm を 12.1.0 に固定、`npx ctxjev-mcp` をすべて版指定。CI に `pnpm audit` を追加 | 6b、9、8b |
| 10 | 主指標の識別力不足・標本の少なさ | ラウンド2へ | — | 設計提案 第3節（新しい事前登録ホールドアウト） | 3a、2a |
| 11 | 重複IDで全体停止・紛らわしいエラー文 | 修正済み | `59af6a3` | テスト「parseClaudeCodeTranscript > keeps the last of records that repeat a uuid, and says so」「keeps the last tool_result when one tool_use_id gets two」「parseTranscript > reports where a broken JSON file breaks…」「jsonErrorOffset」5件、「preCompact.js (dist) > scores a transcript that repeats a record id…」。実際の Claude Code の記録で重複が起きるかは未確認のまま | 1c |
| 12 | MCP がキー無しで即終了 | 修正済み | `6b0432a` | 再現：`initialize` に答える前に終了コード1。修正後、テスト「MCP server without TYPESAFE_API_KEY」3件と「dist/index.js without TYPESAFE_API_KEY > starts, lists its tools…」 | 7b |
| 13 | 評価ハーネスの隔離・Node の版 | 一部修正（CI での自己検査・Node 22 の明記）。Bash の隔離は**ラウンド2へ** | `f7fd254` | CI に `eval-harness` ジョブ（`verify.mjs`・`tasks.mjs --selftest`・`check-docs.mjs`）。`.nvmrc`＝22。Node 20 で実行すると「Node 22 以上が必要」と表示して終了（以前は `globSync` の読み込みエラー） | 3c、8b、6b |
| 14 | 既定 recency の意味の分かりにくさ | 一部修正（文書化のみ。API の変更はラウンド2） | `b2ad630` | README・core/CLI の README・`scorer` と `DEFAULT_POLICY` の説明に「既定では goal を使わず、既定の閾値で古い約30%を削除・次の30%を要約候補にする（最初の依頼も含む）」と明記 | 2b、7a |
| 15 | 小さな重複・黙った既定 | 修正済み | `a4b1a2a` `0f5ffae` | テスト「scoreEntries > rejects a scorer name it does not know…」。日中韓の2文字分割・`quote()`・疑似乱数を core に集約（`splitCjkBigrams`・`quoteAsData`・`seededRandom`）。保存済みの評価基準値が変わらないことを確認（テスト「seededRandom > matches the generator the saved eval results were computed with」、`run.mjs` の出力が同一） | 4b |
| 16 | 毎プロンプトの Node 起動 | ラウンド2へ | — | Windows のフック実行環境で動く前段判定の方法を確認してから行う（設計提案 第2節） | 4c |
| 17 | 貢献・脆弱性報告の窓口 | 修正済み | `2c04d4f` | `CONTRIBUTING.md`（リリース方針を含む）、`SECURITY.md` | 9 |
| 18 | サロゲートの片割れ | 修正済み | `1790caa` | 再現：`truncate('😀'×10, 6)` の結果に単独の `\ud83d`。テスト「cutting text short never splits a character」3件（ZWJ で結合した家族の絵文字や国旗も分割しない） | 1c |

## 監査レポート第4節の個別指摘の対応表

第7節の番号に対応するものはその番号を示します。

| 指摘 | 状態 | 対応（コミット） |
| --- | --- | --- |
| 4.1-1 削ったのに会話が増える | 修正済み | #5（`7da04b0`） |
| 4.1-2 15万件でクラッシュ | 修正済み | #5（`7da04b0`）。再現には `targetTokens` の指定が必要だった |
| 4.1-3 公開版で Quick Start が動かない | 一部修正／保留 | #3。README に明記（`b2ad630`）。0.6.0 の公開はあなたの操作 |
| 4.1-4 `--version` の記載誤り | 修正済み | #8（`15727d2`）。記載を消すのではなく実装 |
| 4.1-5 重複IDで全体停止 | 修正済み | #11（`59af6a3`） |
| 4.1-6 壊れたJSONの紛らわしいエラー | 修正済み | #11（`59af6a3`）。例：「line 3, column 15」を表示 |
| 4.1-7 絵文字の途中切断 | 修正済み | #18（`1790caa`） |
| 4.2-1 主指標で差がない | ラウンド2へ | 事実として変わらない。新しいホールドアウトで再評価（設計提案 第3節） |
| 4.2-2 副指標で Jev がキーワード一致とランダム順に負ける | 一部修正 | 開示を修正（#1、`b81bc76`）。保存した再実行（25% 予算）でも <!-- generated:changes-holdout-retention -->Jev 23.6%、ランダム順 26.5%、キーワード一致 28.3%、切り捨て 0.0%<!-- /generated:changes-holdout-retention -->。効果そのものは**ラウンド2へ** |
| 4.2-3 dev でも効果が小さい | ラウンド2へ | 事実の記録として README・設計提案に掲載（生成） |
| 4.2-4 既定では goal を無視、古い約30%を削除 | 一部修正 | #14（文書化、`b2ad630`） |
| 4.2-5 `local` が既定閾値で重要項目をほぼ全削除 | ラウンド2へ | #6 |
| 4.2-6 プラグインのオフライン代替が最重要の結論を残さない | ラウンド2へ | #6 と同じく採点方式の再設計で扱う |
| 4.3-1 主指標は原理的に差が出にくい、反復が独立でない | ラウンド2へ | #10。反復ごとの再採点を計画に明記 |
| 4.3-2 副指標の比較相手が構造的に0% | 一部修正 | 再現：<!-- generated:changes-cut-analysis -->ホールドアウト 6 会話では切り取り点以降がトークンの 22%〜43%、そこにしかない事実は各 0 件。dev の記録 10 会話では各 1 件<!-- /generated:changes-cut-analysis -->。探索的な「修正依頼まで」の指標を追加し、事前登録の指標と並べて表示（`630072e` `b81bc76`）。事前登録の計画・指標は書き換えていない |
| 4.3-3 数値の食い違い・元データ欠落 | 修正済み | #1。README の +15.6 は削除し、保存した再実行の数値を生成で表示。記録の残っていない過去2回は「検証不能」と明記して残した |
| 4.3-4 古い記述・存在しない文言の引用 | 修正済み | #8。PREREGISTRATION.md の誤引用を正しい文言に修正し、その旨を「登録後の変更」に記録（`b81bc76`） |
| 4.3-5 その他の限界（作者が課題作成、1モデル、AI 採点未検証、近似トークン） | ラウンド2へ | 設計提案 第3節（別エージェントによる課題作成、人手の抜き取り検査） |
| 4.3-6 評価ハーネスの Bash が隔離されていない | ラウンド2へ | #13 の残り。`SECURITY.md` に既知の制限として明記 |
| 4.4-1 テストが型検査対象外 | 修正済み | #7（`041fa38`） |
| 4.4-2 小さな重複 | 修正済み | #15（`0f5ffae`） |
| 4.4-3 未知の scorer が黙って recency | 修正済み | #15（`a4b1a2a`） |
| 4.4-4 CLI キャッシュが無制限・平文 | 修正済み | #4（`739c63a`）。上限 20,000 件 |
| 4.4-5 全プロンプトで Node 起動 | ラウンド2へ | #16 |
| 4.4-6 性能・費用の表現が実測と合わない | 修正済み | #8（`b2ad630`）。主張を削除・訂正（実測で裏付けたわけではない） |
| 4.5-1 Jev クライアント・MCP 本体がオフラインで未テスト | 修正済み | #7（`90296a9` `6b0432a`） |
| 4.5-2 既定 recency の直接テストなし | 修正済み | #7（`90296a9`） |
| 4.5-3 今回の発見が未テスト | 修正済み（`local` 閾値を除く） | 各修正コミットに回帰テスト。`local` の閾値はラウンド2 |
| 4.5-4 「ネットワーク不要」のテストが実送信 | 修正済み | #7（`198e626`）。新しいテストは要求がローカルに届くことを確認しており、旧テストが外部へ送っていたことの裏付けにもなる |
| 4.5-5 サブプロセスが `process.env` を丸ごと継承 | 修正済み | #7（`198e626`） |
| 4.6-1 マスキングの見逃し | 修正済み | #2（`c8cd4ee`）。注：レポートは40形式すべてを列挙していないため、指摘された形式と既存の対応形式から表を再構成した。旧実装はこの表で 20/40（レポートでは 19/40） |
| 4.6-2 切り詰め後マスクの部分漏洩 | 修正済み | #2（`f93362c`） |
| 4.6-3 CLI キャッシュが平文・誰でも読める | 修正済み | #4（`739c63a`） |
| 4.6-4 set-goal を未マスクで保存 | 修正済み | #4（`616a38c`） |
| 4.6-5 利用者プロジェクト内の再帰削除 | 修正済み | #4（`616a38c`）。`.ctxjev/preserved/mydata/notes.txt` が残るテストを追加 |
| 4.6-6 供給網 | 一部修正 | #9（`a19f2d3` `b2a18fc` `7619a61`）。開発依存の脆弱性・Actions の固定・npm の固定・npx の版指定は修正。MCP SDK が引き込む本番依存の数（94）は変えていない |
| 4.6-7 評価ハーネスが隔離なしでコマンド実行 | ラウンド2へ | #13 の残り |
| 4.7-1 README が未リリース版を現状として説明 | 一部修正／保留 | #3（`b2ad630`）。公開はあなたの操作 |
| 4.7-2 冒頭説明が既定 recency と矛盾 | 修正済み | `b2ad630` |
| 4.7-3 出力例が既定では再現しない | 修正済み | `b2ad630`。2026-09-24 に実際に取得した出力に差し替え |
| 4.7-4 古い記述・`--version`・「inspection skills が2つ」 | 修正済み | #8 |
| 4.7-5 MCP がキー無しで即終了、`.mcp.json` がビルド前は存在しないパス | 修正済み | #12（`6b0432a`）。MCP README に「`.mcp.json` はビルドが必要」と追記 |
| 4.7-6 未知オプションのエラー文、壊れた `last-run.json` の誤解を招く表示 | 修正済み | `15727d2`（例：「unknown option '--scorr' for `ctxjev analyze` — see `ctxjev --help`」）、`e9341d8`（「Last run: unknown — last-run.json isn't valid JSON」） |
| 4.8-1 プラグインがリリース手続きを経ずに配布される | ラウンド2へ | 調査済み（`git-subdir` 取得元＋`release` ブランチ案）。今切り替えると 0.5.0 へのダウングレードになるため見送り |
| 4.8-2 版のずれ（npm 0.5.0、リポジトリ 0.6.0、タグ） | 保留 | 公開はあなたの操作。README に明記。版番号は変更していない（指示どおり） |
| 4.8-3 CI に型検査・監査・依存更新・自己検査がない | 修正済み（カバレッジ計測を除く） | `041fa38` `b2a18fc` `f7fd254` `7619a61`。カバレッジ（テストが実行したコードの割合）の計測はラウンド2 |
| 4.8-4 評価スクリプトが Node 22 専用 | 修正済み | #13（`f7fd254`）。Node 22 を必要条件として明記し、分かりやすく停止させる（Node 20 対応はしていない） |
| 4.9 CONTRIBUTING/SECURITY なし、依存が古い、保守者1名、短期間に多数の公開 | 一部修正 | #17（`2c04d4f`）、#9。保守体制（バス係数）はコードでは解決できない。リリースをまとめる方針は `CONTRIBUTING.md` に明文化 |
| 第9節「Jev を使う経路はすべて未検証」 | 修正済み | ライブテスト14件合格、`eval/run.mjs --gate --runs 3` 合格（dev） |
| 第9節「約100ms」「50件でも同じ費用」「84%/95%」は未検証 | 修正済み | 前二者は主張を削除・訂正。後者は保存した実行（`retention-dev.json`）から生成した数値に置き換え |

「再現せず」と判定した指摘はありませんでした。再現の条件が監査の記述と異なったもの（4.1-2 は `targetTokens` 指定時のみ、4.6-1 は再構成した表で 20/40）は上に注記しています。

## 評価結果について隠さず記録しておくこと

- ホールドアウトの副指標（事前登録の指標、25% 予算）は、保存した再実行で <!-- generated:changes-holdout-retention -->Jev 23.6%、ランダム順 26.5%、キーワード一致 28.3%、切り捨て 0.0%<!-- /generated:changes-holdout-retention --> でした（`eval/results/retention-holdout.json`）。**Jev はランダム順より少ない事実しか残していません。** README の冒頭と「Does It Work?」に明記しました。
- 同じコードでコミット前に行った最初の実行（`eval/results/superseded/retention-holdout-precommit.json`、由来は同じフォルダの README）では <!-- generated:changes-precommit -->Jev 22.6%（Jev − 切り捨て +22.6 [+8.0, +37.5]）<!-- /generated:changes-precommit --> でした。保存した実行の中でも3回の値は大きく揺れています（設計提案 1.2 節）。過去に保存されなかった実行（PREREGISTRATION.md の「検証不能」と明記した表の2回と、以前の README の値）もそれぞれ別の値で、Jev の揺れが大きいことを示しています。
- 探索的に追加した「修正依頼まで」の保持率では <!-- generated:changes-at-cut -->Jev 41.1%、切り捨て 31.0%、ランダム順 33.5%（Jev − ランダム順 +7.6 [−2.4, +19.7]）<!-- /generated:changes-at-cut --> で、Jev − ランダム順の区間は 0 をまたいでいます。この指標は結果を見た後に追加したもので、何かを確認する証拠にはなりません。
- ホールドアウトはすでに見て分析したため、ここから先は新しい主張の確認に使えません。README・PREREGISTRATION.md・設計提案に明記しました。

## あなたにお願いしたいこと

1. **プルリクエストの確認とマージ**（下書きとして作成）。CI の3つのジョブ（テスト、評価ハーネス、依存の監査）が通ることを確認してください。
2. **GitHub の「非公開の脆弱性報告」を有効にする**：リポジトリの Settings → Security → Private vulnerability reporting。`SECURITY.md` はこの窓口を案内しています。
3. **0.6.0 を公開するとき**：①`node scripts/check-versions.mjs --update-pins` で MCP 設定例の固定版を 0.6.0 にする、②公開ワークフローを実行する（ライブテストと評価ゲートは自動で走ります）。プラグイン配布の固定（`release` ブランチ方式）を同時に行うかは、設計提案の質問5で決めてください。
4. **設計提案の5つの質問への回答**（[`docs/design/round-2-scoring-and-evaluation.md`](../design/round-2-scoring-and-evaluation.md) 第4節）。

## 自己推定スコア

**これは私自身による見積もりです。独立した再監査で確認していただく必要があります。** 監査の配点表に沿って項目ごとに見積もりました（内訳は設計提案 第5節の表）。

| 大項目（配点） | 監査時 | ラウンド1後（自己推定） |
| --- | --- | --- |
| 1. 中核機能と正しさ（15） | 11.0 | 13.5 |
| 2. 有効性（10） | 3.5 | 3.75 |
| 3. 評価の厳密さと主張の誠実さ（10） | 5.5 | 7.4 |
| 4. コード品質と設計（15） | 12.5 | 14.0 |
| 5. テスト（15） | 10.5 | 13.5 |
| 6. セキュリティとプライバシー（10） | 5.5 | 8.25 |
| 7. ドキュメントと使い勝手（10） | 6.0 | 8.25 |
| 8. パッケージ・連携・CI/CD・リリース（10） | 7.5 | 8.5 |
| 9. プロジェクトの健全性（5） | 3.0 | 4.0 |
| **合計（100）** | **65.0** | **約 81（自己推定、幅をみて 77〜84）** |

最も大きく伸びていないのは「2. 有効性」です。Jev が単純な方法より成果を良くするという証拠はラウンド1では増えておらず（副指標ではむしろランダム順以下であることを明記しました）、この項目は新しい評価の結果次第です。
