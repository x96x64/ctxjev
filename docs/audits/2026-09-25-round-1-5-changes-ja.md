# ラウンド1.5の変更記録と自己評価（2026-09-25）

この文書は、ラウンド1.5（2回目の独立監査を受けた作業）で**何を変えたか、その証拠、やっていないこと**をまとめた記録です。監査の指摘ごとの分類（修正済み／未修正／再現せず）と再現手順は [`2026-09-25-audit-2-triage-ja.md`](2026-09-25-audit-2-triage-ja.md)（以下「仕分け文書」）にあり、この文書はそれを作業の側から見たものです。

- 対象：PR A（[#8](https://github.com/x96x64/ctxjev/pull/8)、ブランチ `claude/ctxjev-round-1-5-fdk0ik`）と PR B（[#9](https://github.com/x96x64/ctxjev/pull/9)、`claude/ctxjev-round-1-5-fdk0ik-release-060`、下書きのまま。マージは保守者が行う）。
- 出発点：`main` の `0e19b39`。コミットは短いハッシュで示します。
- 評価の数値はこの文書には書いていません。数値は README・事前登録文書・設計文書・仕分け文書にあり、どれも `packages/core/eval/check-docs.mjs` が保存済みの結果ファイルから生成しています。

## 用語メモ（初出のみ）

- **テスト**：コードが期待どおりに動くかを自動で確かめる小さなプログラム。`pnpm test` で全部を実行します。
- **CI**：GitHub が変更のたびに自動で実行する検査（テスト、型チェック、文書の数値検査など）。
- **dist**：ソースから作った配布用のファイル。プラグインの dist はリポジトリに含めています。
- **マスク**：送信前に、秘密情報らしい文字列を `[REDACTED]` に置き換えること。
- **ライブ確認**：本物の Jev の API を実際に呼ぶ確認（有料。ごく少額）。
- **ホールドアウト／dev**：最終判定用に取っておく評価データ／設計に使ってよい評価データ。

## 1. やさしい言葉での要約

1. **消えかけていた評価結果を取り戻しました。** 2026-09-24 に、プラグインの効果を未知の課題で測る事前登録の比較が**2回、最後まで実行されていた**のに、`main` には「結果が出なかった」と書かれていました。結果ファイル4つを1バイトも変えずに復元し、2回分の結果を両方、生成で文書に載せました。どちらの回も「効果は示せず」です。
2. **監査で見つかった問題のうち、コードで直せるものを直しました。** 秘密情報のマスクの穴（PGP 秘密鍵、カード番号、ツール名、ID）、MCP の入力の長さ制限、プラグインのスキルの権限、「最新ターンを触らない」の実装、オフライン採点（`local`）の削りすぎ、CLI の表示、再注入する抜粋の引用の強化、CI の権限、公開は `main` からだけ、などです。直すたびに、変更前に問題が再現することを確かめ、回帰テスト（同じ問題が戻ったら失敗するテスト）を足しました。
3. **プラグインは既定で何も外部に送らなくなりました。** Jev で採点した抜粋に効果が示せていないため、既定をオフライン採点にし、Jev は `CTXJEV_SCORER=jev` で選ぶ方式にしました。
4. **ラウンド2の評価の準備をしました。** 新しい課題の形式（本物の変更履歴、元のリポジトリとの比較、日本語の課題には日本語の質問）、エージェントのコマンドを隔離して動かす仕組み、AI の採点をあなたが確かめるための日本語のページ、ラウンド2の計画（費用の確認点つき）を用意しました。**お金のかかる評価は1つも実行していません。**
5. **0.6.0 の公開の準備をしました（公開はしていません）。** CHANGELOG を確定し、公開後に初めて正しくなる変更は PR B に分け、クリック手順の手順書を書きました。

## 2. 作業項目ごとの状況

「状況」の意味：**完了**＝コミットとテストか出力で示せる。**一部**＝理由つきで残りがある。**未着手**＝理由は第7節。

### 2.1 手順0〜1（監査の保存、アーカイブの復元、仕分け）

| 項目 | 状況 | コミット | 証拠 |
| --- | --- | --- | --- |
| 監査レポートを原文のまま保存 | 完了 | `9b58483` | `docs/audits/2026-09-25-audit-2-ja.md` |
| アーカイブの結果ファイル4つを中身を変えずに復元 | 完了 | `5811861` | git のブロブのハッシュが元のコミットのものと一致（`results/README.md` の表）。コミットメッセージにタグと SHA |
| 2回のプラグインのホールドアウト比較を両方、生成で掲載 | 完了 | `ac07729` | README・事前登録文書・プラグイン README・設計文書の生成ブロック。`check-docs.mjs` が CI で一致を検査 |
| アーカイブの別案の事前登録を採用せず、選択肢として記録。止めた経緯と集めたデータを開示 | 完了 | `7a482a6` | 設計文書 3.10 節、仕分け文書 1.6 節 |
| `RUBRIC.md` を移植しない判断と理由 | 完了 | `7a482a6` | 仕分け文書 1.5 節 |
| アーカイブの README・ROADMAP の直しのうち未反映のもの | 完了 | `546e9dc` | 仕分け文書 1.7 節 |
| 監査の第4・6・7節の全指摘の仕分け | 完了 | `7a482a6` | 仕分け文書（「再現せず」は0件。未修正は付録 A に再現のコマンドと出力） |

### 2.2 手順2（修正の一覧）

| 項目 | 状況 | コミット | 証拠（テスト名など） |
| --- | --- | --- | --- |
| ツール名のマスク、ID を要求ごとの仮の名前（`e0`, `e1`…）に | 完了 | `02ebdcb` | 「sends neither an entry id nor an unmasked tool name」。ライブ確認（第5節）で、本物の Jev の答えが正しい項目に戻ることも確認 |
| PGP 秘密鍵ブロック、カード番号を表形式のテストに | 完了 | `02ebdcb` | `packages/core/test/redactCases.ts` に追加。変更前は両方ともそのまま残ることを再現 |
| （途中で発見）アンダースコアの直後のトークンが漏れる | 完了 | `02ebdcb` | 表の「GitHub token after an underscore」 |
| MCP の `id`・`toolName` の長さ上限、CHANGELOG 0.1.9 の言い過ぎの訂正 | 完了 | `5af206b` | 「rejects an id or a tool name over its length cap」「bounds every string it accepts」 |
| `set-goal`・`status` スキルの権限を `dist/status.js` の実行だけに | 完了 | `e8c6adb` | `skills.test.ts`。Claude Code 2.1.282 で `${CLAUDE_PLUGIN_ROOT}` が展開されることを確認 |
| 「最新ターンを触らない」（`protectLastTurn`、既定オン）と `protectLast` の下限の維持 | 完了 | `1682fe4` | 監査と同じ形（最後の指示の後にツール3往復）のテストほか4件と性質テスト。README の記述も更新 |
| CLI：独自形式での `--protect-last` をエラーに、採点方式ごとの凡例、マイナスの節約を表示しない | 完了 | `09d4f44`・`0e076ef` | 「rejects --protect-last and --no-protect-last-turn…」「says what the score means for each scorer」「…never reports a non-positive saving as saved」 |
| 完了していないツール呼び出しが元の位置に残る | 完了 | `b108f81` | 「keeps unfinished tool calls where they were made, in order, not after later entries」 |
| `local` の点数をバッチ内の順位に変換。較正は dev だけ | 完了 | `8e17b6b`・`326d813` | 規則を実行前に決めてから dev だけで較正し、結果を `results/local-calibration-dev.json` に保存（dev と明記）。ホールドアウトは使っていない |
| 再注入する抜粋の引用を抜け出せないように | 完了（強化。根本解決ではない） | `0e2efe4` | 「keeps an excerpt that tries to issue instructions inside its own quoted line」 |
| `ci.yml` を `permissions: contents: read` に、公開は `main` からだけ | 完了 | `f51e0a0` | ワークフローの `if:` と最初の拒否ステップ |
| Actions の SHA 固定と npm の版固定の確認 | 完了（確認のみ） | ラウンド1 `b2a18fc` | 全ワークフローの `uses:` が 40 桁の SHA、`npm install -g npm@12.1.0` |
| 古いコメント、名前と中身が逆のテスト、README のセッション数 | 完了 | `aa33096` | セッション数は生成に置き換え |
| プラグインの既定をオフライン採点に（アーカイブから意図して移植） | 完了 | `522a9d5` | 「scores offline by default and sends nothing, even with TYPESAFE_API_KEY set」（偽の Jev サーバーに要求が1件も届かない） |
| Issue テンプレート | 完了 | `fa4feae` | `.github/ISSUE_TEMPLATE/` |
| `UserPromptSubmit` のフックが毎回 Node を起動する | 未着手（第7節） | — | 仕分け文書 付録 A-6 |

### 2.3 手順3（ラウンド2の評価の準備）

| 項目 | 状況 | コミット | 証拠 |
| --- | --- | --- | --- |
| 保持率の指標 v2（修正依頼まで）を追加し、v1 は変えずに名前を付ける | 完了 | `2783a65` | `run.mjs` が v1・v2 を名前つきで出力。v1 は dev のオフライン実行で変更前と同一の結果 |
| 新しい課題の形式（本物の履歴の差分、元のリポジトリとの比較、日本語の質問） | 完了 | `eed1bb7` | `harness-selftest.mjs`（CI）、`verify.mjs`、`AUTHORING.md` |
| 既存の記録やデータは書き換えず、問題を記録（`room-booking` など） | 完了 | `7a482a6` | 設計文書 補足A、仕分け文書 付録 A-2〜A-5 |
| エージェントの隔離（docker、なければ名前空間の隔離、Mac は `sandbox-exec`） | 完了 | `b660e49`・`6ff2a4b`・`876ef0b` | 自己検査（外への通信、ホームの読み取り、リポジトリ外への書き込みなど）が CI の GitHub の実行環境で合格。独立レビューで見つかった穴（行き先の無いリンクを通した書き込み）を `6ff2a4b` で修正し、自己検査に5種類目として追加。防げること・防げないことは設計文書 第7節 |
| 人の確認用の日本語ページ（コマンド1つ、進み具合の保存、JSON の書き出し） | 完了 | `23e1b1e` | `node packages/core/eval/review.mjs --demo`。手順は設計文書 第8節 |
| 設計文書：決定事項、復元した証拠、2a/2b の計画と費用の確認点 | 完了 | `7a482a6` | `docs/design/round-2-scoring-and-evaluation.md` 第0・6節（確認点の金額は生成） |

### 2.4 手順4（0.6.0 の準備）

| 項目 | 状況 | コミット | 証拠 |
| --- | --- | --- | --- |
| CHANGELOG 0.6.0 の確定（PR A では見出しは「unreleased」） | 完了 | `7e43f2c` | CHANGELOG.md |
| プラグインをリリースのタグから配る仕組み（検査と書き換え） | 完了 | `f0db92d` | `scripts/check-versions.mjs`。Claude Code 2.1.282 で、同じ形で既存タグ `v0.5.0` から実際にインストールできることを確認 |
| リリースの手順書（クリック手順） | 完了 | `7e43f2c`・`689fa4d` | `docs/release/0.6.0-checklist-ja.md` |
| 公開後に正しくなる変更（版の固定、マーケットプレイスのタグ、README、CHANGELOG の日付） | 完了（PR B） | PR B `bc947f8` | PR B でだけ `check-versions.mjs` が合格（PR A では意図して不合格） |
| タグ作成、npm への公開、公開ワークフローの実行 | 実施せず（指示どおり） | — | 手順書の手順2 |

## 3. 期待値を変えたテストと、弱めていない理由

削除したテストはありません。既存のテストの期待値を変えたのは次の場合だけで、どれも検査の強さは同じか強くなっています。

| テスト | 変更 | 弱めていない理由 |
| --- | --- | --- |
| `jevClient.test.ts`「asks one question per entry…」 | 質問の名前の期待を「項目の ID」から「`e0`, `e1`…」に | 仕様の変更そのもの（ID を送らない）。本文が順に正しく入っていることの検査を追加 |
| `jevClient.test.ts` の偽の Jev | 答えを「ID」ではなく「本文」で決めるように | 要求に ID が無くなったため。答えが正しい項目に戻ることは、本文で見分けて引き続き検査 |
| `anthropicMessages.test.ts` のツールの対のテスト | 採点を `local` から固定の点数に | 検査の対象（対が壊れないこと、同じ3件が消えること）は同じ。`local` の新しい挙動は `prune.test.ts` で別に検査 |
| `anthropicMessages.test.ts` の 15 万件のテスト | `protectLastTurn: false` を指定 | このテストの目的は「10 万件以上を消せること」。既定では会話全体が最新ターンになり何も消えないため。既定の挙動は新しいテスト群で検査 |
| 同じファイルの性質テスト | `protectLastTurn` を乱数でオン・オフ | オンのときに「最新ターンが変わらない」を検査に追加（強化） |
| `preCompact.test.ts` の2件 | `CTXJEV_SCORER=jev` を指定し、名前を変更 | 既定がオフラインになったため、Jev を選んだ場合の失敗時の動きを引き続き検査。注記に `CTXJEV_SCORER=jev` が含まれることの検査を追加。既定の動きは新しいテストで検査 |
| `select.live.test.ts` | 採点方式 `'jev'` を明示 | 既定が `'local'` になったため。ライブテストが Jev を測り続けるため |
| `cli/src/prune.test.ts` | 期待する文言を「only apply」から「only applies」に | 文法の直し（1つのオプション名に続くため）。検査の強さは同じ |
| `cache.test.ts` の1件 | 名前を中身に合わせた | 監査 4.4-5b。検査内容は変更なし |
| `cli/src/report.test.ts` の凡例のテスト | `local` の凡例の期待する文言を「…ranked within this transcript, blended with recency」に（`0e076ef`） | 期待する文言が長くなった（より厳しい）。凡例が実際の点数（新しさを混ぜた値）と合うように直した |
| `redact.test.ts` の件数 | 40 → 51 | 形式の追加のみ（削除なし） |

## 4. `pnpm test` から除外したもの（`8b81ea6`）

- **除外したもの**：`packages/core` の `eval/fixtures/**` だけです（`packages/core/package.json` の `"test": "vitest run --exclude \"eval/fixtures/**\""`）。該当するのは、ラウンド2の課題形式の見本（`eval/fixtures/tasks/format2-demo/`）にある2つのファイルです。
  - `template/test/cart.test.js`：課題の雛形のテスト。課題を解くまで**わざと失敗する**もので、`node:test` 用に書かれています。
  - `hidden/acceptance.test.js`：隠しテスト。準備した課題のリポジトリの中でだけ動く（`../src/cart.js` はそこにしか無い）もの。
  - どちらも `harness-selftest.mjs`（CI の `eval-harness`）が、本来の場所で実行して検査しています。
- **本物のテストの数は減っていません**：同じコードで、除外の前後を比べました（API キーなし、`TYPESAFE_BASE_URL` は閉じたローカルポート）。

  | | テストファイル | テスト |
  | --- | --- | --- |
  | 除外前（`vitest run`） | 16 合格、2 スキップ、**2 失敗**（上の2ファイル） | 242 合格、8 スキップ |
  | 除外後 | 16 合格、2 スキップ | 242 合格、8 スキップ |

  違いは見本の2ファイルだけです。`main` には `src/` の外にテストの名前のファイルが無いので、`main` で実行されていたテストも変わりません。スキップの8件は API キーが必要なライブテストです。

## 5. ライブ確認と費用

- **Jev のライブ確認（1回）**：新しい要求の形式を、合成データ `examples/sample-transcripts/checkout-bug.json` だけで確かめました。
  - 要求の中の項目の名前は `e0`〜`e6` で、元の ID（`e1`〜`e7`）とはずれています。本物の Jev の答えは、それぞれの項目に位置で正しく戻りました（例：元の `e3` は要求の `e2` の答えを受け取る）。正解ラベルで関連ありの3件はどれも関連なしの4件より高い点でした。ID で戻していたら、元の `e3` は要求の `e3`（別の項目）の低い点を受け取っていたはずです。
  - ツール名は送信前にマスクを通ることを、オフラインでも確認（キーらしいツール名は `mcp__[REDACTED]` として送られる）。
  - 使用量：入力 859 トークン、出力 123 トークン。費用は約 **$0.000036**（Jev の公開価格、入力 100 万トークンあたり $0.042、出力は無料）。
- **Anthropic の API（Claude）**：**$0**。有料のエージェント評価は一度も実行していません。
- **このラウンドの合計**：約 $0.000036。

## 6. 最終チェックの結果

API キーを外し、Jev の接続先を閉じたローカルポート（`TYPESAFE_BASE_URL=http://127.0.0.1:9`）にして、手元（Linux、Node 22）で実行しました。PR A は独立レビューの指摘を直したあとの `6c6d461` と、再確認の指摘を直した `876ef0b`（どちらの後のコミットもこの文書だけの変更で、文書の数値検査は再実行して合格）、PR B はその PR A（この文書を含む）を取り込んだ版です。

| 検査 | PR A | PR B |
| --- | --- | --- |
| `pnpm install --frozen-lockfile`（ロックファイルどおりに入れる） | 合格 | 合格 |
| `pnpm build` | 合格 | 合格 |
| プラグインの dist が最新か（ビルド後に差分がない） | 合格 | 合格 |
| `pnpm lint` | 合格 | 合格 |
| `pnpm typecheck`（テストの型チェックを含む） | 合格 | 合格 |
| `pnpm test` | 合格（core 242・mcp-server 18・claude-plugin 59・cli 58 件、計 377 件合格。スキップ 14 件はキーが必要なライブテスト） | 合格（同じ） |
| `pnpm audit --audit-level high`（依存の脆弱性） | 合格（脆弱性 0） | 合格（脆弱性 0） |
| `check-docs.mjs`（文書の評価の数値が保存データと一致） | 合格 | 合格 |
| `verify.mjs`（全課題：雛形は隠しテストに不合格、解答は合格） | 合格 | 合格 |
| `harness-selftest.mjs`・`check-sessions.mjs` | 合格 | 合格 |
| `tasks.mjs --selftest`（`unshare` の隔離、16課題） | 合格（脱出の試み 5/5 を拒否、FIFO も拒否） | 合格（同じ） |
| `tasks.mjs --selftest`（`docker` の隔離） | 合格（手元の組み立てイメージで1課題。CI では `node:22-bookworm` で毎回実行） | CI で実行 |
| `check-versions.mjs`（版番号とタグ固定） | **不合格（意図どおり）**：公開後に正しくなる変更は PR B にあるため | 合格 |
| GitHub の CI（`test (20)`・`test (22)`・`eval-harness`・`audit`） | 最終版で確認（最後の報告に記載） | 同左 |

## 7. 独立レビューの指摘と対応

作業の内容を見ていない別の AI（サブエージェント）に、元の依頼文（あとから届いた追加の指示を含む）と PR A の差分だけを渡し、依頼文とそのルールに照らしてレビューさせました（`34ae099` が対象）。こちらの考えや説明は渡していません。レビュー役は、アーカイブの結果ファイルのハッシュの一致、テストの数、手順ごとの主張をコマンドで確かめています。

| # | 重さ | 指摘 | 対応 |
| --- | --- | --- | --- |
| 1 | **必須** | 評価用エージェントの書き込み（Write）が、行き先のまだ無いシンボリックリンクを通すと、複製の外にファイルを作れる。設計文書は「防げる」と書いていた | **修正**（`6ff2a4b`）。修正前に再現（外にファイルができた）。リンクをたどった実際のパスだけを使い、行き先の無いリンクは拒否、最後の一歩はリンクをたどらずに開く。自己検査に5種類目として追加し、修正前のコードでは失敗（4/5）、修正後は16課題すべて合格（5/5）。設計文書 第7節を訂正し、Mac の `sandbox-exec` に残る理論上の穴も明記 |
| 2 | 任意（境界） | プラグインのホールドアウトの1行の要約が、見栄えのよい方の条件だけを載せている（4つの点推定はすべて 0 以下） | **修正**（`6c6d461`）。両方の実行の両方の条件と、点推定の向きを生成で載せる形に |
| 3 | 任意 | 手書きの数値：プラグインの2回の実行の費用（コミットメッセージから引用）と、仕分け文書 1.6 節のアーカイブの文章からの引用 | **修正せず**。どちらも評価の結果ファイルに無い数値で、出典（コミットメッセージ、アーカイブの文章）を明記した引用です。「数値は保存データから生成」の例外として、ここに記録します |
| 4 | 任意 | この文書の第6・7節が未記入 | **修正**（この版で記入） |
| 5 | 任意 | レビュー用ページの手順が Mac 専用で、初めての `git` で出る画面の説明がない | **修正**（`6c6d461`）。Mac の「コマンドライン・デベロッパ・ツール」の画面と、Windows での方法を追記 |
| 6 | 任意 | CLI の `local` の凡例が、新しさ（recency）を混ぜていることを書いていない | **修正**（`0e076ef`） |
| 7 | 任意 | `lastTurnStart()` は、ツールの結果の横に置かれた文章（リマインダーなど）も新しい指示とみなすため、守られる「最新ターン」が実際の最後の指示より後から始まることがある | **修正せず（記録）**。守る範囲が狭くなる方向の限界です。第8節に記載 |
| 8 | 任意 | `holdout2` の検査は、確かめる記録が1件も無いときは必ず合格する | **修正せず（記録）**。今は holdout2 の記録がまだ無いので、何も確かめられないのは正しい動きです。記録を作る 2a で、ラベルの有無も確かめるようにします（第8節） |
| 9 | 任意 | 隔離に使う docker のイメージ `node:22-bookworm` が、中身が変わりうる名前で指定されている | **修正せず（記録）**。本番の評価（2b）の前に、中身を固定する指定（ダイジェスト）に変えます（第8節） |

**再確認**：修正（`6ff2a4b`〜`affe1d0`）を同じレビュー役にもう一度確かめさせました。必須の指摘は無く、指摘1が閉じていること（行き先の無いリンク、複製の外を指すフォルダのリンク、複製の中のリンク経由の Edit・Grep・Glob、エラーメッセージに実際のパスが出ないこと）と、新しい自己検査が修正前のコードで失敗することを、レビュー役自身が確かめました。任意の指摘は2件で、どちらも直しました。

| # | 重さ | 指摘 | 対応 |
| --- | --- | --- | --- |
| 10 | 任意 | 設計文書 第7節の Mac の限界が「書き込み」だけに触れているが、「読み取り」にも同じ理論上の穴がある | **修正**（`876ef0b`）。「読み書き」に直し、リンクをたどらないのは書き込みの最後の一歩だけと明記 |
| 11 | 任意（以前からある問題） | エージェントが複製の中に FIFO（名前付きパイプ）を作ると、ファイル操作の道具が開いた時点で評価の仕組み全体が止まる | **修正**（`876ef0b`）。通常のファイル以外は読み書きを拒否。自己検査に追加し、修正前のコードでは止まる（20秒で打ち切り）、修正後は16課題すべて合格 |

レビュー役が「問題なし」と確かめた主な点：復元した4つのファイルが元と1バイトも違わないこと、アーカイブのコミットを丸ごと取り込んでいないこと、2回のプラグインの結果を両方載せていること、テストの削除や弱体化がないこと（`main` から PR A で core 208→242、cli 54→58、mcp-server 16→18、claude-plugin 51→59 件）、vitest の除外が見本の2ファイルだけで本物のテストの数が変わらないこと、Jev への送信がすべて `buildJevRequest()` を通ること、秘密情報や実物の会話ログが追加されていないこと、ホールドアウトを調整に使っていないこと。

## 8. やっていないこと、残っていること

- **`UserPromptSubmit` のフックが毎回 Node を起動する（監査 4.4-4）**：未修正。`UserPromptExpansion` フックで絞り込める見込みですが、古い版の Claude Code と Windows での挙動を本物で確かめてから切り替えます（設計文書 第2節末）。
- **カバレッジ（テストが通ったコードの割合）の CI での計測**：未修正（仕分け文書 付録 A-7）。
- **効果の証拠**：まだありません。プラグインは2回とも「効果は示せず」、保持率のホールドアウトでは Jev がランダム順を下回っています。ラウンド2の新しい事前登録の評価で判断します。**古いホールドアウトは、新しいことを確かめる証拠には使えません。**
- **0.6.0 の公開、タグ、プラグインのタグ配信への切り替え**：指示どおり実施していません。保守者が手順書に従って行います。
- **再注入の引用の強化**は根本的な解決ではありません（AI が引用の中の文を指示と取り違える可能性はゼロにはなりません）。
- **ラベル付けの複数化**：未着手（ラウンド2で、課題の作成者の分離と人の確認を先に行います）。
- **独立レビューの任意の指摘のうち直していないもの**（第7節の 3・7・8・9）：`lastTurnStart()` がリマインダーなどの文章も指示とみなす点、`holdout2` の検査が空のときに合格する点（2a で記録を作るときにラベルの有無も確かめる）、docker のイメージの固定（2b の前に固定）。

## 9. 自己評価（再監査が必要）

**これは作業した本人（AI）による見積もりで、独立した監査ではありません。** 監査2と同じ内訳で見積もりました。監査2の 62 点は、ラウンド1より前のコード（`2cf4eba`）に対する点数です。

| 大項目（配点） | 監査2 | 自己評価 | 主な理由 |
| --- | ---: | ---: | --- |
| 1. 中核機能と正しさ（15） | 11 | 13.5 | README の出力例、`--version`、`local` の削りすぎ、マイナスの節約、最新ターン、重複記録、黙って無視されるオプションを修正 |
| 2. 有効性（10） | 3.5 | 4 | 効果の証拠は変わらずありません（プラグインは2回とも示せず）。プラグインが既定で送信しなくなった分だけ |
| 3. 評価の厳密さと主張の誠実さ（10） | 5 | 7 | 復元した2回の結果を両方掲載、全数値を生成、v2 指標、新しい課題形式と隔離。主要評価の検出力はまだ示せていない |
| 4. コード品質と設計（15） | 12 | 13 | テストの型チェック、キャッシュ、古いコメントを修正。毎回の Node の起動は残る |
| 5. テスト（15） | 10.5 | 13 | 監査で見つかった問題すべてに回帰テスト。カバレッジは CI で計測していない |
| 6. セキュリティとプライバシー（10） | 4.5 | 8 | マスクは 51 形式、ツール名と ID、キャッシュの権限、MCP の長さ制限、スキルの権限、CI の権限。マスクは経験則による |
| 7. 文書と UX（10） | 6 | 7 | CLI の表示を修正。npm は 0.6.0 の公開まで README と食い違う（PR B で解消） |
| 8. 配布、CI/CD、リリース（10） | 7 | 8 | SHA 固定、監査、`main` からだけ公開。プラグインのタグ配信は PR B のマージまで効かない |
| 9. 保守性（5） | 2.5 | 3.5 | CONTRIBUTING、SECURITY、Issue テンプレート、依存の更新。作者1名・歴史が短い点は変わらない |
| **合計** | **62** | **77** | |

- 0.6.0 の公開と PR B のマージが手順書どおりに済めば、7・8 の項目がそれぞれ約1点上がる見込みです（合計 79 前後）。
- 最も配点の重い「効果の証拠」は、ラウンド2の評価を実行するまで上がりません。監査の目標（95 点）には、ラウンド2で効果を示すことが必要です。
- この見積もりは甘くなりがちです。**独立した再監査で確かめてください**（手順書の最後）。
