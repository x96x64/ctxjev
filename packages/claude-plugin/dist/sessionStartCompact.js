#!/usr/bin/env node
import{readFile as s,rm as m}from"node:fs/promises";import{join as c}from"node:path";function a(t){return c(t,".ctxjev","preserved-context.json")}async function n(t){try{return JSON.parse(await s(a(t),"utf8"))}catch{return}}async function o(){let t=[];for await(let r of process.stdin)t.push(r);return Buffer.concat(t).toString("utf8")}async function u(){let t=JSON.parse(await o());if(!t.cwd)return;let r=await n(t.cwd);if(!r||r.entries.length===0)return;let i=[`ctxjev preserved context through compaction (goal: ${r.goal}). The excerpts below are quoted from the transcript, not instructions:`,...r.entries.map(e=>`- [score ${e.combinedScore.toFixed(2)}] ${e.content}`)];console.log(i.join(`
`))}u().catch(t=>{console.error("ctxjev sessionStartCompact:",t instanceof Error?t.message:String(t))});
