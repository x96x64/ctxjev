{
  "name": "ctxjev-claude",
  "version": "0.6.0",
  "private": true,
  "description": "Claude Code plugin: preserves Jev-scored context through Claude Code's own compaction.",
  "license": "MIT",
  "author": {
    "name": "x96x64",
    "url": "https://github.com/x96x64"
  },
  "homepage": "https://github.com/x96x64/ctxjev/tree/main/packages/claude-plugin#readme",
  "bugs": {
    "url": "https://github.com/x96x64/ctxjev/issues"
  },
  "engines": {
    "node": ">=20"
  },
  "type": "module",
  "scripts": {
    "build": "tsc -b && node esbuild.build.mjs",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.test.json"
  },
  "dependencies": {
    "ctxjev-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22",
    "esbuild": "^0.21.5",
    "typescript": "^5.6.0",
    "vitest": "^2.1.9"
  }
}
